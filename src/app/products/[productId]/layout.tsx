import React from "react";

const ProductDeatilsLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <>
      {children}
      <h2>ProductDeatilsLayout</h2>
    </>
  );
};

export default ProductDeatilsLayout;
